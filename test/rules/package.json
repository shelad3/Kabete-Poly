{
  "name": "firestore-rules-tests",
  "version": "1.0.0",
  "description": "Firestore rules emulator tests for Kabete Poly",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore 'node --test rules.test.js'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.1"
  }
}