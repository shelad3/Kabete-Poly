{
  "name": "kabete-poly-functions",
  "description": "Cloud Functions for Kabete Poly app push notifications",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "firebase emulators:exec --only firestore 'VOTING_SALT=test-salt FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 node --test test/*.test.js'"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true,
  "devDependencies": {
    "firebase-functions-test": "^3.5.0"
  }
}
